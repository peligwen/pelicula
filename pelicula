#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
BIN=./bin/pelicula
if [[ ! -x "$BIN" ]] || [[ cmd/pelicula/main.go -nt "$BIN" ]]; then
    echo "Building pelicula CLI..." >&2
    mkdir -p bin
    ( cd cmd/pelicula && go build -o ../../bin/pelicula . )
fi
exec "$BIN" "$@"
