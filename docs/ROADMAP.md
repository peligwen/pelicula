# Pelicula Roadmap

Pelicula's core phases (A–F) are shipped. This file tracks what's next, what's deferred, and summarises what landed.

---

## Active

_Nothing in active development — v0.1 scope is complete._

---

## Deferred

- **Invite Apprise notification**: notify admin via Apprise/internal feed when an invite is claimed. Deferred; low priority since the dashboard shows active invites and redemption history.

- **Retire/retention/storage pruning**: storage management and dedup reporting. Deferred, no timeline.
- **NFS-backed library (named volumes)**: host `movies/` and `tv/` on a NAS via NFS without a macOS Finder mount. Docker Desktop's Linux VM mounts the export directly through `local` volumes with `driver_opts: type=nfs`, so containers read/write it as normal named volumes — no `/Volumes`, no VirtioFS, no FUSE. Keep `WORK_DIR` (downloads + processing) local because NFS breaks hardlinks and is poorly suited to active torrent I/O; accept that Sonarr/Radarr will fall back to copy-on-import. Shape: new `compose/docker-compose.nfs.yml` + `compose/docker-compose.local-library.yml` override pair; `LIBRARY_NFS` / `NFS_HOST` / `NFS_EXPORT` / `NFS_OPTIONS` in `.env`; ``pelicula up`` picks the right overlay. Full plan: `~/.claude/plans/shiny-floating-cosmos.md`.

---

## Shipped

**CLI Code-Quality Sweep (2026-04-26):** Three-commit pass over the `cmd/pelicula/` Go CLI (3,340 LOC across 25 source files, stdlib-only) targeting correctness, cross-platform safety, duplication, and test coverage — the third in a series of rolling section sweeps (prior entries: Autowire & Default-Features Sweep, Procula Pipeline Hardening). Correctness fixes (`f6aa253`): `cmdRedeploy` in `cmd_rebuild.go` mutated the range-loop copy variable rather than `targets[i]`, so `pelicula redeploy middleware` was silently a no-op — the `docker compose build` step was called with the literal string `"middleware"` (no such image) rather than `"pelicula-api"`; fixed with an index loop. `cmdDoctor` in `cmd_doctor.go` called bare `exec.Command` for Docker and Compose version probes, bypassing the `ctx.Plat.NeedsSudo` flag; `pelicula check` therefore always reported versions as unavailable on Synology — fixed by routing through a new `Compose.DockerRaw` helper. Migration 5 in `env.go` converted `REMOTE_ACCESS_ENABLED=true` to `REMOTE_MODE=portforward` but did not handle the `=false` direction, did not delete the old key, and left two downstream readers (`cmd_up.go`, `remote.go`) still checking the legacy key — so remote-mode status was always false on fresh installs; fixed with `isRemoteEnabled(env)` helper in `helpers.go` and a corrected Migration 5 that handles both directions and deletes the stale key. `ensureStackDown` in `cmd_reset.go` derived active profiles from the current `.env`, so a reset after clearing the WireGuard key left gluetun/qBittorrent/Prowlarr containers running (the same leak `cmd_down.go` was patched for previously); fixed by hardcoding `[]string{"vpn","apprise"}` and adding `--remove-orphans`, matching the existing `cmd_down.go` behavior. Minor cleanup: collapsed the dead `_ = err` inside `if err != nil` in `cmd_logs.go` to `_ = c.Run(...)`, and replaced three string-concatenated default paths in `platform.go` with `filepath.Join` calls so Windows deployments produce valid paths. Dedup / extraction (`36b39e6`): `lanIP()` in `cmd_up.go` enumerated all non-loopback IPv4 addresses without RFC1918 filtering, paralleling `detectLANIPs()` in `platform.go` with divergent semantics; `lanIP` now delegates to `detectLANIPs` with a length guard, removing the parallel logic and closing TESTGAP-5 for free. `checkAuthError(*http.Response)` extracted to `helpers.go`, replacing the identical 401/403 block copy-pasted in `cmd_export.go` and `cmd_import.go`. `peliculaBaseURL(env)` extracted to `helpers.go`, replacing five duplicated `"http://localhost:" + env["PELICULA_PORT"]` constructions across export, import, status, up, and VPN commands, with a fallback to port `7354` when `PELICULA_PORT` is empty. Test coverage (`d44342c`): 4 table-driven cases + `TestIsRemoteEnabled` (5 cases) added to `env_test.go` covering the full REMOTE migration matrix — would have caught BUG-3 (missing `=false` direction, stale-key not deleted, legacy-key reader). `walkUpForMarker` extracted from `getScriptDir` as an injectable pure function; 3 path cases + smoke test added to `helpers_test.go`, guarding the binary's first-boot directory resolution. `firstExistingAncestor` in `dirs.go` covered with 5 `t.TempDir()` cases in `dirs_test.go` (happy path, root sentinel, non-existent input). Test file count: 5 → 7 (new `helpers_test.go`, `dirs_test.go`; `env_test.go` extended). No `cmd/pelicula/go.mod` changes; no CLI external contract changes (subcommand names, flags, exit codes unchanged). Operator-visible bug fixes: `pelicula redeploy middleware`, `pelicula check` on Synology, remote-mode status display on fresh installs, and `pelicula reset-config` VPN container leak.

**Procula Pipeline Hardening (2026-04-25):** Five-commit pass on procula's robustness, defaults, and longevity. Hard wall-clock cap on the FFmpeg transcode subprocess (`max(60m, min(24h, expected_runtime_minutes × 6))`) so a pathological input can no longer hang the single sequential worker; deadline-exceeded classified permanent via `errTranscodeTimeout` sentinel. Permanent-error list audit adds `permission denied`, `invalid argument`, `read-only file system` — all three would previously have looped forever in the transient retry ladder. Crash-loop separation: container restarts and OOM kills now increment `interrupt_count` (new schema v10 column) with a constant 30s backoff; only after 3 consecutive interruptions does the next event count toward `retry_count`, so a redeploy bump no longer burns a job's budget. Storage-critical back-pressure: `POST /api/procula/jobs` returns `503 + Retry-After: 300` once the worst filesystem crosses 95%; hysteresis clears at the warning threshold (85%) so the gate doesn't flap. Notifications table bounded to 90 days OR newest 1000 rows via `pruneNotifications` on the existing 24h archive ticker (also runs once at startup). ROADMAP Phase E reconciled with code reality (3 profiles ship `Enabled=true`, gated by master `TRANSCODING_ENABLED` switch); explicit default test pins added so future drift is loud.

**Autowire & Default-Features Sweep (2026-04-25):** Six-commit pass over autowire correctness and default-feature posture. Drift-correcting `wireDownloadClient` / `wireImportWebhook` / `bazarrAlreadyWired` so container restarts converge instead of leaving stale config. Hot-path `*arr` wire payloads moved to typed `arr.{DownloadClient,Notification,Application,ReleaseProfile}Resource` structs in `internal/clients/arr/types.go`. Jellyfin defaults: `KnownProxies` seeded (or `PELICULA_KNOWN_PROXIES` override) so X-Forwarded-For is trusted by remote-access auth; `QuickConnect` re-asserted off; `DLNA` seeded off; `ServerName` populated from `PELICULA_DASHBOARD_NAME` / hostname when empty; hardware-acceleration auto-probe at first wire (env > VAAPI `/dev/dri/renderD128` > VideoToolbox arm64-darwin > none; user-set values never overridden). qBittorrent: queueing limits seeded (3 active downloads / 8 active torrents / slow-torrent ignore on); RSS auto-downloader + session re-asserted off; DHT/PeX/LSD off by default with `PELICULA_QBIT_PRIVATE_PEERS=false` opt-out. *arr "Pelicula" release profile demotes REMUX / 2160p / HDR10+ / DV by default to keep small NAS installs from grabbing 80GB releases (`PELICULA_DEFAULT_RELEASE_PROFILE=false` opts out without deleting an edited profile).

**Peligrosa — Security Hardening:** Auth is always on; `PELICULA_AUTH=off` removed. Jellyfin-backed auth with loopback auto-session for host-machine admin access. `middleware/internal/peligrosa/` subpackage extracts auth, invites, requests, user CRUD, and webhook validation. Central CSRF middleware (`requireLocalOriginStrict` / `requireLocalOriginSoft`) wired per-route. X-Real-IP trust hardened to socket-peer CIDR. Remote role capping (remote vhost injects `X-Pelicula-Remote: true`; middleware caps to viewer). Open LAN registration (`PELICULA_OPEN_REGISTRATION`). First-admin password generated by setup wizard. See [PELIGROSA.md](PELIGROSA.md) for threat model.

**Dashboard Consolidation:** Retired the standalone Procula UI (`/procula/`) — all pipeline visibility is now in the main dashboard. Services grid removed in favour of sidebar-only services list. New Settings tab centralises pipeline toggles, subtitle settings, notification mode, and download defaults. Search cards are click-to-expand with detail chips (rating, certification, runtime, network, genres). New "monitoring" pipeline lane shows grabbed requests that haven't started downloading yet. In-page job drawer replaces the Procula link (shows validation checks, file info, transcode details, timeline). Event log with filter chips and pagination. Password auto-generated on the register page with visibility toggle.

**Infrastructure:** Prowlarr now runs on gluetun's network namespace (alongside qBittorrent) so all indexer traffic is VPN-routed. Prowlarr is reachable at `gluetun:9696`; nginx updated accordingly.

**CLI additions:** `pelicula redeploy [svc]` rebuilds Docker images then does a full stack down/up (distinct from `rebuild` which only restarts). After `pelicula up`, the CLI polls for first-run state and auto-opens the registration page in the browser if no admin is registered yet.

**Pre-v1.0 Hardening:** SQLite data layer for all mutable state (`modernc.org/sqlite` via pure-Go driver). Migration framework with `PRAGMA user_version` for both middleware and procula. Auto-migration from JSON files on first startup (idempotent, handles corrupt files). Configurable service URLs via environment variables (`SONARR_URL`, `RADARR_URL`, etc.). Versioned backup format (v1→v2) with forward-compatible import chain; v2 includes roles, invites, and requests. Go CLI rewrite (`cmd/pelicula/`) — single binary, cross-platform (macOS/Linux/Windows/Synology), stdlib-only, replaces the bash script. API contract freeze with stability policy (additive-only changes).

**Phase A — Onboarding:** Two-prompt setup (VPN key + country), `--advanced` walkthrough, `the Settings UI` runtime menu, `set_env_var` helper, `$CONFIG_DIR/pelicula/` directory.

**Phase B — Auth & Roles:** Jellyfin-backed auth with viewer / manager / admin roles, `Guard` / `GuardManager` / `GuardAdmin` middleware, dashboard login form, role-based UI hiding. Post-ship hardening: CSRF origin check, `MaxBytesReader`, username and UUID validation. Legacy `off` mode later removed in the Peligrosa extraction.

**Phase C — In-Dashboard Notifications:** Procula catalog stage writes to `/config/procula/notifications_feed.json` (ring buffer, 50 events), bell icon with unread badge, Processing section on dashboard with job cards and progress bars.

**Phase D — Request Queue:** First-party viewer request queue built into the dashboard. Viewers request from search; admins approve/deny with configurable Radarr/Sonarr quality profiles. Apprise notifies on state change. Import webhook auto-transitions requests to "available".

**Phase E — Transcoding:** `procula/process.go` with FFmpeg progress tracking (parses `time=` from stderr) and profile matching on codec or resolution. Three default profiles ship enabled (Compatibility 1080p, Compatibility 720p, Downscale 4K to 1080p) — they remain inert until the user opts into transcoding via `TRANSCODING_ENABLED=true`.

**Phase F — External Notifications (Apprise):** Apprise container (opt-in Docker Compose profile), `direct` mode for single-webhook setups (ntfy, Gotify, any webhook URL), config at `/config/procula/notifications.json`. Discord is not a supported provider.

**Invite Flow (Phase D follow-up):** One-time shareable invite links for admin-free user onboarding. `POST /api/pelicula/invites` generates a 32-byte random base64url token (stored in `/config/pelicula/invites.json`) with configurable TTL (default 7 days) and optional max-uses cap. Admins create links via the dashboard Users section ("Create invite link" button). `/register` (static HTML, no Pelicula session required) renders a username+password form that submits to `POST /api/pelicula/invites/{token}/redeem`, creating the Jellyfin account on success. Expired, exhausted, and revoked tokens return clear error states. Admin can revoke or delete tokens from the dashboard.

**Bazarr — Subtitle Acquisition:** Bazarr container wired into the stack alongside Sonarr/Radarr. Auto-wire creates a language profile from `PELICULA_SUB_LANGS` (set via `the Settings UI` → Subtitles) and connects Bazarr to both *arr apps on startup. nginx proxies `/bazarr`. Procula flags imports with missing subtitles, then actively kicks Bazarr via its API to search immediately (rather than waiting for Bazarr's background polling cycle), waits up to 30 minutes for sidecars to appear, and proceeds to dual-sub generation once they arrive.

**Dual Subtitles:** Procula pipeline stage that generates stacked ASS sidecar files (`Movie.en-es.ass`) alongside source media. Base language (familiar) appears bottom-center in white; secondary (learning) appears top-center in yellow. Source cues are extracted from embedded subtitle streams or `.{lang}.srt` / `.{lang}.ass` sidecars; Argos Translate (offline, not bundled in image) synthesizes missing tracks. Cue alignment is base-anchored (secondary cue midpoint must fall within base cue range). Configurable via env vars (`DUALSUB_ENABLED`, `DUALSUB_PAIRS`, `DUALSUB_TRANSLATOR`) and Procula settings UI. Known limitations: no bitmap (PGS/DVD) sub support; font fallback required for Arial; per-title opt-out not yet implemented.
